// Web stub — SQLite is not available in the browser.
// The app uses in-memory state only on web (for preview purposes).

export const db = null as any;

export async function initDatabase() {
  // no-op on web
}
